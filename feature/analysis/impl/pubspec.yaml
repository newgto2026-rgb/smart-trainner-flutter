name: smart_trainner_feature_analysis_impl
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  smart_trainner_core_domain:
    path: ../../../core/domain
  smart_trainner_core_model:
    path: ../../../core/model
  smart_trainner_feature_analysis_api:
    path: ../api
  smart_trainner_feature_analysis_domain:
    path: ../domain

dev_dependencies:
  lints: ^5.1.1
  flutter_test:
    sdk: flutter
