name: smart_trainner_feature_workout_domain
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  smart_trainner_core_domain:
    path: ../../../core/domain
  smart_trainner_core_model:
    path: ../../../core/model

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
