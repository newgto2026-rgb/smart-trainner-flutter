import 'package:flutter/widgets.dart';

class TrainingRoute extends StatelessWidget {
  const TrainingRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
