name: smart_trainner_core_database
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  collection: ^1.19.1
  sqflite_common: ^2.5.6
  smart_trainner_core_model:
    path: ../model

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
