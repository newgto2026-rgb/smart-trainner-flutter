name: smart_trainner_core_domain
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  collection: ^1.19.1
  smart_trainner_core_model:
    path: ../model

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
