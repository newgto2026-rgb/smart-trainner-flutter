name: smart_trainner_core_datastore
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  smart_trainner_core_model:
    path: ../model

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
