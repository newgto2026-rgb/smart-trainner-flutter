name: smart_trainner_core_data
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  smart_trainner_core_database:
    path: ../database
  smart_trainner_core_datastore:
    path: ../datastore
  smart_trainner_core_domain:
    path: ../domain
  smart_trainner_core_model:
    path: ../model
  smart_trainner_core_network:
    path: ../network

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
